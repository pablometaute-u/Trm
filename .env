# ============================================================
# Configuración de la API — MonedaValor
# Copia este archivo como .env y completa los valores reales
# ============================================================

# --- Azure SQL Database ---
DB_SERVER=sltrm.database.windows.net
DB_NAME=sldbtrm
DB_USER=Juan
DB_PASSWORD=Admin1234
DB_DRIVER=ODBC Driver 18 for SQL Server

# --- Autenticación ---
# Genera una key segura, por ejemplo con: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=sap_connect
